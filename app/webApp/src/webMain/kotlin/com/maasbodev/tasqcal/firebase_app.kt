package com.maasbodev.tasqcal

// Firebase JS interop removed — firebase is initialized via CDN module (resources/firebase-init.js).
// Keeping this file as a placeholder to avoid accidental re-introduction of JS-only APIs in multiplatform source sets.
