package com.maasbodev.tasqcal

// Analytics interop removed — analytics is initialized via CDN module (resources/firebase-init.js).
// Placeholder file to avoid introducing JS-only code into multiplatform sources.
